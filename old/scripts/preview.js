// todo: read top x files from articles/ directory (ordered alphabetically, so the newest date is first
// then call formatFile(filename) for each :) ?

function formatFile(fileName) {
	// todo
	// use the title as link text, link to file
	// use the custom date tag to display date
	// include the first paragraph
}
